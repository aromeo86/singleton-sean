<UserControl x:Class="SimpleTrader.WPF.Controls.MajorIndexListing"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SimpleTrader.WPF.Controls"
             mc:Ignorable="d" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock FontSize="18"
                   Grid.Row="0"
                   Text="Major Indexes"/>

        <Grid Grid.Row="1"
              Margin="0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <local:MajorIndexCard DataContext="{Binding DowJones}"
                                  Grid.Column="0"
                                  Margin="10"/>

            <local:MajorIndexCard DataContext="{Binding Nasdaq}"
                                  Grid.Column="1"
                                  Margin="10"/>

            <local:MajorIndexCard DataContext="{Binding SP500}"
                                  Grid.Column="2"
                                  Margin="10"/>
        </Grid>
    </Grid>
</UserControl>
