<UserControl x:Class="YouTubeViewers.WPF.Views.EditYouTubeViewerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:components="clr-namespace:YouTubeViewers.WPF.Components"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:YouTubeViewers.WPF.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Style="{StaticResource PageHeader}"
                   Text="Edit YouTube Viewer"/>

        <components:YouTubeViewerDetailsForm DataContext="{Binding YouTubeViewerDetailsFormViewModel}"
                                             Grid.Row="1"
                                             Margin="0,30,0,0"/>
    </Grid>
</UserControl>
